---
interface Props {
  // 'dark' renders the wordmark in white (for dark backgrounds)
  variant?: 'light' | 'dark';
}
const { variant = 'light' } = Astro.props;
const text = variant === 'dark' ? 'text-white' : 'text-slate-900';
---
<a href="/" class={`flex items-center gap-2.5 font-bold ${text} text-lg`}>
  <div class="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center flex-shrink-0">
    <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
      <rect x="3" y="3.5" width="12" height="1.8" rx="0.9" fill="white"/>
      <rect x="3" y="8" width="8" height="1.8" rx="0.9" fill="white" opacity=".75"/>
      <rect x="3" y="12.5" width="10" height="1.8" rx="0.9" fill="white" opacity=".5"/>
    </svg>
  </div>
  <span data-company>Portfolio Desk</span>
</a>
