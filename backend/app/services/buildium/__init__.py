"""Buildium Open API migration connector.

* :mod:`app.services.buildium.client` — thin authenticated HTTP client.
* :mod:`app.services.buildium.migration_service` — orchestrates the ordered,
  idempotent migration of Buildium data into Portfolio Desk.
"""
